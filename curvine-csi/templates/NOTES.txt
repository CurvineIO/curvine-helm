Curvine CSI has been installed.

Release:
  Name: {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}
  CSI Driver: {{ .Values.csiDriver.name }}

Verify the release:
  kubectl get csidriver {{ .Values.csiDriver.name }}
  kubectl get deployment -n {{ .Release.Namespace }} {{ .Values.controller.name }}
  kubectl get daemonset -n {{ .Release.Namespace }} {{ .Values.node.name }}
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

Inspect generated service accounts and RBAC:
  kubectl get sa -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl get clusterrole,clusterrolebinding -l app.kubernetes.io/instance={{ .Release.Name }}

Create a StorageClass after the runtime cluster is reachable.
