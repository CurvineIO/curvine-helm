apiVersion: {{ ternary "apps.kruise.io/v1beta1" "apps/v1" .Values.openKruise.enabled }}
kind: StatefulSet
metadata:
  name: {{ include "curvine.workerFullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "curvine.workerLabels" . | nindent 4 }}
  {{- if and .Values.openKruise.enabled .Values.openKruise.persistentPodState.autoGenerate }}
  annotations:
    kruise.io/auto-generate-persistent-pod-state: "true"
    {{- if .Values.openKruise.persistentPodState.preferredPersistentTopology }}
    kruise.io/preferred-persistent-topology: {{ .Values.openKruise.persistentPodState.preferredPersistentTopology | quote }}
    {{- end }}
    {{- if .Values.openKruise.persistentPodState.requiredPersistentTopology }}
    kruise.io/required-persistent-topology: {{ .Values.openKruise.persistentPodState.requiredPersistentTopology | quote }}
    {{- end }}
  {{- end }}
spec:
  serviceName: {{ include "curvine.workerFullname" . }}
  replicas: {{ .Values.worker.replicas }}
  selector:
    matchLabels:
      {{- include "curvine.workerSelectorLabels" . | nindent 6 }}
  podManagementPolicy: Parallel
  updateStrategy:
    type: RollingUpdate
    {{- if .Values.openKruise.enabled }}
    rollingUpdate:
      podUpdatePolicy: {{ .Values.openKruise.podUpdatePolicy }}
    {{- end }}
  template:
    metadata:
      labels:
        {{- include "curvine.workerSelectorLabels" . | nindent 8 }}
        {{- with .Values.worker.labels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        # Trigger pod restart when ConfigMap changes
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.worker.annotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "curvine.serviceAccountName" . }}
      {{- include "curvine.imagePullSecrets" . | nindent 6 }}
      {{- if .Values.openKruise.enabled }}
      readinessGates:
        - conditionType: InPlaceUpdateReady
      {{- end }}
      {{- if .Values.worker.hostNetwork }}
      hostNetwork: true
      dnsPolicy: {{ .Values.worker.dnsPolicy }}
      {{- end }}
      containers:
        - name: worker
          image: {{ include "curvine.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /entrypoint.sh
          args:
            - worker
            - start
          env:
            - name: APP_HOME
              value: {{ include "curvine.appHome" . | quote }}
            - name: CURVINE_HOME
              value: {{ include "curvine.home" . | quote }}
            - name: CURVINE_CONF_FILE
              value: {{ include "curvine.configFile" . | quote }}
            - name: ORPC_BIND_HOSTNAME
              value: "0.0.0.0"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: POD_CLUSTER_DOMAIN
              value: {{ .Values.global.clusterDomain | quote }}
            - name: CURVINE_MASTER_HOSTNAME
              value: "{{ include "curvine.masterFullname" . }}-0.{{ include "curvine.masterServiceName" . }}.{{ .Release.Namespace }}.svc.{{ .Values.global.clusterDomain }}"
            - name: CURVINE_WORKER_HOSTNAME
              {{- if .Values.worker.usePodIPAsHostname }}
              value: "$(POD_IP)"
              {{- else }}
              value: "$(POD_NAME).{{ include "curvine.workerFullname" . }}.{{ .Release.Namespace }}.svc.{{ .Values.global.clusterDomain }}"
              {{- end }}
            {{- with .Values.worker.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          ports:
            - name: rpc
              containerPort: {{ .Values.worker.rpcPort }}
              protocol: TCP
            - name: web
              containerPort: {{ .Values.worker.webPort }}
              protocol: TCP
            {{- if .Values.worker.s3Gateway.enabled }}
            - name: s3
              containerPort: {{ include "curvine.s3GatewayPort" . | int }}
              protocol: TCP
            {{- end }}
          livenessProbe:
            tcpSocket:
              port: {{ .Values.worker.rpcPort }}
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: {{ .Values.worker.rpcPort }}
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          {{- if .Values.worker.privileged }}
          securityContext:
            privileged: true
          {{- end }}
          resources:
            {{- toYaml .Values.worker.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: {{ include "curvine.configFile" . }}
              subPath: curvine-cluster.toml
              readOnly: true
            - name: env
              mountPath: {{ include "curvine.envFile" . }}
              subPath: curvine-env.sh
              readOnly: true
            {{- range .Values.worker.storage.dataDirs }}
            {{- if .enabled }}
            - name: {{ .name }}
              mountPath: {{ include "curvine.absPath" (dict "path" .mountPath) }}
            {{- end }}
            {{- end }}
            - name: logs
              mountPath: {{ .Values.config.log.logDir }}
            {{- with .Values.worker.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "curvine.fullname" . }}-config
        - name: env
          configMap:
            name: {{ include "curvine.fullname" . }}-config
        - name: logs
          emptyDir: {}
        {{- range .Values.worker.storage.dataDirs }}
        {{- if .enabled }}
        {{- if .hostPath }}
        - name: {{ .name }}
          hostPath:
            path: {{ .hostPath }}
            type: DirectoryOrCreate
        {{- end }}
        {{- end }}
        {{- end }}
        {{- with .Values.worker.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.worker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if or .Values.worker.antiAffinity.enabled .Values.worker.affinity }}
      affinity:
        {{- include "curvine.workerAntiAffinity" . | nindent 8 }}
        {{- with .Values.worker.affinity }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      {{- with .Values.worker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  {{- $hasDataDirs := false }}
  {{- range .Values.worker.storage.dataDirs }}
  {{- if and .enabled (not .hostPath) }}
  {{- $hasDataDirs = true }}
  {{- end }}
  {{- end }}
  {{- if $hasDataDirs }}
  volumeClaimTemplates:
    {{- range .Values.worker.storage.dataDirs }}
    {{- if and .enabled (not .hostPath) }}
    - metadata:
        name: {{ .name }}
        labels:
          {{- include "curvine.workerLabels" $ | nindent 10 }}
      spec:
        accessModes:
          - ReadWriteOnce
        {{- if .storageClass }}
        storageClassName: {{ .storageClass }}
        {{- end }}
        resources:
          requests:
            storage: {{ .size }}
    {{- end }}
    {{- end }}
  {{- end }}
